$(function () {
    $("#datepicker").datepicker();
    // $('.app-appoinment-popup').hide();
    $('.selector').on('click', function () {
        var elem = $('.selector');
        var x = elem.offset().left;
        var y = elem.offset().top;
        console.log('x: ' + x + ' y: ' + y);
        $('.app-appoinment-popup').css('position','absolute').css('top',y).css('left',x)
    })
});